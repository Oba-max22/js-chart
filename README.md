# js-chart

Write a JS function that accepts a variable number of arguments, multiplies through and return the result

Write a JS function that makes a remote API call. Transforms the retrieved date and plot either Bar chart or line chart using Tensorflow.js visor.



you can use the publically available endpoint:

https://jsonplaceholder.typicode.com/users